To run the React app follow the bellow instructions and to start the node application check the Readme file in node repo.

1. Clone the repo in your local.
2. Once cloned run the command npm install.
3. Add the correct port number that you used for node app in the variable 'apiURL' in function postApi in common.js.
4. Give the command npm start to run the React application.